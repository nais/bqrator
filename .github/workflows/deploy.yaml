name: Build and deploy
on:
  push:
    branches:
      - main
    paths-ignore:
      - "*.md"
      - "*.env"
  pull_request:
    types: [opened, synchronize, labeled]

permissions:
  contents: write
  id-token: write

jobs:
  build-deploy:
    uses: nais/actions/.github/workflows/mise-build-deploy-fasit.yaml@5c203609f918d351959ca792314bd68dae207287 # ratchet:nais/actions/.github/workflows/mise-build-deploy-fasit.yaml@main
    with:
      builds-chart: true
      deploys-to-fasit: true
      targets: |
        [
          { "target": { "kind": "tenant", "tenant": "ci-nais" }, "wait": true },
          { "target": { "kind": "tenant" } }
        ]
      deploy-pr-targets: |
        [ { "target": { "kind": "tenant", "tenant": "dev-nais" } } ]
      mise-tasks: '["test", "check"]'
    secrets:
      NAIS_IO_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.NAIS_IO_WORKLOAD_IDENTITY_PROVIDER }}
